var msg = "hello world";
console.log(msg);

var http = require('http');
http.createServer();

/*  If you change something, and then click on the file twice in vscode, you get a diff. Neat!
    you can use the icon in the bottom left to create a new branch. also neat!
    In order to be able to push vscode to github, you need to configure it to a remote repository via the terminal (good ol fashoned way)

    git remote add origin https://github.com/user/repo.git

    git remote -v

    It worked, sweet.
*/